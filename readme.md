# Проект «Sail Club»

Учебный проект сайта яхт-клуба «Под парусом». Включает в себя семантическую разметку, работу с графикой, шрифтами и стилизацию контентных блоков.

## 📋 Описание задачи
Проект выполнен в рамках учебного курса. Основной упор сделан на точность верстки по макету, соблюдение семантики HTML5 и корректную организацию файловой структуры для автоматизированных проверок.

## 🛠 Выполненные этапы

### 1. Семантическая разметка (v2.0)
- Реализована структура: `header`, `main` (5 разделов), `footer`.
- **Обновление:** Добавлен новый раздел **«Новости»** (блок новостей), расположенный между статьями и контактами.
- Все блоки размечены с использованием семантических тегов (`section`, `article` и т.д.).

### 2. Работа с графикой и контентом
- Экспортированы все изображения из макета (логотипы, фотографии, иконки) в форматах **SVG** и **JPG/PNG**.
- В тегах `<img>` строго соблюдены размеры `width` и `height` согласно макету.
- Подключены шрифты из библиотеки **Google Fonts**.

### 3. Стилизация (CSS)
- Создана базовая стилизация всех 5 блоков в файле `css/style.css`.
- Использованы относительные пути для подключения стилей и графических ресурсов.

### 4. Построение сетки (Flexbox)
- Реализована **флекс-сетка** для крупных блоков страницы (до 3-го уровня вложенности).
- Настроены горизонтальные и вертикальные отступы, колонки и размеры блоков согласно макету.
- Использованы специальные сеточные обводки для визуального контроля и отладки макета.

## 📂 Структура проекта
- `index.html` — главная страница с 5-ю разделами в `main`.
- `css/style.css` — таблицы стилей.
- `img/` — графические ресурсы проекта (включая графику для блока новостей).

## 🚀 Как запустить
1. Скачайте проект или склонируйте репозиторий.
2. Откройте `index.html` в браузере.
